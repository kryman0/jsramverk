checks:
  javascript: true

build:
  nodes:
    jsramverk-frontend:
      dependencies:
        override:
          - "nvm install v13.13.0"
      environment:
        selenium: true
      tests:
        override:
          # - npm install
          - npm run test-scrutinizer
            # coverage:
            #   file: build/coverage/clover.xml
            #   format: clover